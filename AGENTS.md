# lifelaw-monitor-web — Project Instructions

## 1. 목적과 범위

- 이 저장소는 **생활법령 모니터링 관리자 Web**이다. React 기반 관리자 UI와
  FastAPI 기반 관리 백엔드를 포함한다.
- `lifelaw-monitor`(수집기)와 **독립된 저장소이자 독립된 배포 단위**다.
  Git 이력, 브랜치, 릴리스 주기를 공유하지 않는다.
- 두 시스템은 **PostgreSQL 데이터 계약**으로 연동한다. CUBRID는 R 시스템
  원장이며 이 저장소에서는 기본 read-only다.
- **수집기와 HTTP API로 직접 결합하지 않는다.** Web이 수집기의 프로세스를
  호출하거나 수집기가 Web을 호출하는 동기 경로를 만들지 않는다. 상호작용은
  승인된 DB 계약과 관리 명령 레코드를 통해서만 일어난다.
- 책임 분담:
  - `lifelaw-monitor` — 수집, 추출, 정규화, 비교, 판정, R 반영
  - `lifelaw-monitor-web` — 인증, RBAC, 조회, 정책 관리, 실행 제어 **요청**,
    승인, 감사

상세 설계는 이 파일에 중복해서 적지 않는다. 활성 설계 문서는
`docs/architecture/designs/DESIGN_lifelaw_monitor_web_admin_architecture_v1_2.md`
(v1.2, `APPROVED`)이며, **구현 권위는 승인된 항목으로 한정된다.** 승인 상태의
단일 출처는 해당 문서 §23.1 표다. v1.1 이하 판은 모두 `SUPERSEDED`이며 인용하지
않는다.

**컬럼 단위 DB 계약의 단일 출처는 `docs/contracts/db-contract.md`다.** 설계
문서 §7.1은 그 요약이며, 충돌 시 DDL을 직접 판독한 계약 문서가 우선한다.
컬럼 소유권을 판단할 때 설계 요약만 보고 결정하지 않는다.

2026-08-06 사용자 승인으로 확정된 사항은 다음과 같다.

- 백엔드 **FastAPI(Python)** — 정책 변경 로직을 수집기와 공유 라이브러리로
  재사용하기 위한 전제이므로 언어를 바꾸지 않는다(설계 §2.1)
- 프론트엔드 **React/TypeScript(Vite)**
- 인증 **서버 측 세션 + 쿠키.** 동일 출처 배포, CORS 기본 비활성, CSRF 토큰
  검증, 권한은 매 요청 서버 재조회(설계 §17.4)
- Web 소유 테이블 **`TW_` 접두사 7종.** `TW_`는 Web이 유일한 작성자이며,
  수집기는 `TW_ADMIN_COMMAND`의 상태 전이만 수행한다(설계 §7.2)
- **역할 6종 · 권한 14종 확정.** 권한 문자열은 코드 상수로 고정하고 임의
  문자열을 권한으로 받지 않는다. 추가는 설계 문서 개정 사항이다(설계 §17)

화면 단위 분해는 `docs/architecture/designs/DESIGN_admin_screen_inventory_v0_1.md`
(`PROPOSAL`)를 참조한다. 화면은 명시적 등록물이며, 새 테이블이 생겨도 해당
문서 표에 등록하지 않는 한 UI에 노출하지 않는다.

## 2. 사용자 응답 언어

- 모든 진행 보고, 질문, 오류 보고, 최종 결과는 **한국어**로 작성한다.
  사용자가 영어로 질문해도 한국어로 답한다.
- 코드, 식별자, 명령어, 스키마 이름, 원문 인용은 영어를 유지한다.
- 사실, 추론, 제안, 가정, 미결정을 명시적으로 구분해서 서술한다.

## 3. 권위 순서

```text
1. 사용자 명시 승인
2. 이 저장소에서 승인된 ADR
3. 승인된 용어·코드 정의
4. 승인된 데이터베이스 계약
5. 승인된 보안·RBAC 계약
6. 승인된 컴포넌트 명세
7. DESIGN, RESEARCH, PROMPT 문서
8. 과거·보관 문서
```

- `PROPOSAL`, `ADVISORY`, `REVIEW` 상태 문서는 **사용자 승인 전 구현 권위가
  아니다.** 이 상태의 문서를 근거로 코드, 스키마, 권한, 계약을 확정하지 않는다.
- 같은 계층에서 충돌하면 `사용자 승인 → 더 구체적 문서 → 더 최신 승인 문서 →
  명시적 ADR` 순으로 해소한다. 해소되지 않으면 추론하지 말고 OPEN QUESTION으로
  남기고 중단한다.
- 파일명이 최신처럼 보인다는 이유로 권위 문서로 선택하지 않는다. 문서 내부
  상태 헤더와 등록된 활성 경로를 함께 확인한다.
- 최종 ADR 번호나 승인 문서의 다음 버전 번호를 미리 선점하지 않는다.

## 4. 독립 저장소 경계

- 다른 저장소의 `AGENTS.md`, private instruction, session log, plan, memory를
  이 저장소로 반입하지 않는다. 지침 파일에 대한 저장소 간 심볼릭 링크를 만들지
  않는다.
- `lifelaw-monitor`는 **읽기 전용 참조**다. 해당 저장소의 파일 수정, staging,
  commit, 브랜치 조작을 하지 않는다.
- 저장소 간에는 사용자 승인을 받은 **공개 중립 산출물만** 공유한다.
  - 승인된 설계, 공개 스키마와 데이터 계약, 승인된 ADR, 상태·코드 정의,
    재현 가능한 테스트 결과, 사용자 결정과 명시된 가정
- 다른 저장소에서 온 자료는 작성자가 누구인지와 무관하게 그 자체로 권위가
  아니다. 사용자가 승인하고 이 저장소의 공개 권위 산출물로 편입될 때만
  입력이 된다.
- **복사해 온 문서를 독자적으로 수정한 뒤 새로운 권위라고 주장하지 않는다.**
  원본과 달라진 부분은 출처와 변경 사유를 명시하고 사용자 승인을 받는다.
- 계약 문서를 복사할 때는 원본의 문서 ID와 버전을 함께 기록해 드리프트를
  추적 가능하게 만든다.
- **계약 버전이 불일치하면 fail-closed 한다.** 이 저장소가 기대하는 DB 계약
  버전과 실제 대상 스키마가 다르면 추정해서 계속 진행하지 않는다. 기동
  시점에 불일치를 감지하고 해당 기능을 열지 않은 채 중단·보고한다. 특히
  기대한 컬럼·코드값·생성 컬럼이 없을 때 조용한 폴백을 만들지 않는다.

## 5. 관리 기능 원칙

"모든 테이블 관리"는 **모든 데이터를 조회할 수 있고 필요한 운영 행위를
제공한다**는 뜻이다. **모든 물리 테이블에 무제한 CRUD를 연다는 뜻이 아니다.**

- **Web 소유 테이블만** 일반 CRUD를 허용한다. 단 Web 소유 테이블이라도
  이력·감사·명령 원장 테이블은 아래 append-only 규칙이 우선하며 CRUD 대상이
  아니다.
- 수집 상태는 CRUD가 아니라 **업무 명령(command)** 으로 관리한다.
- `TN_CRAWL_TARGET`, `TH_CRAWL_TARGET`, `TN_BATCH_RUN`의 실행 상태를 Web이
  직접 수정하지 않는다.
- 공통 코드(`TC_COMMON_CODE`)의 임의 추가·수정·삭제를 금지한다. 코드값은
  승인된 용어·코드 정의가 단일 출처이며, Web은 조회해서 표시만 한다.
- **소프트 삭제·사용 여부 플래그로 권위를 우회하지 않는다.** `use_yn`,
  `del_yn` 같은 플래그를 뒤집는 것은 값의 의미를 바꾸는 행위이며 삭제와
  동등하게 취급한다. 공통 코드와 상태 코드에 대해서는 이 플래그 변경도
  금지 대상이고, 승인된 ADR 없이 UI·API에서 노출하지 않는다.
- 이력과 감사 데이터는 **append-only**다. UPDATE와 DELETE 경로를 만들지 않는다.
- CUBRID 쓰기는 승인된 R 계약과 활성화 게이트를 충족하기 전까지 **금지**한다.
  게이트 미충족 상태에서의 write는 0건이어야 한다.
- 브라우저가 데이터베이스에 직접 접근하지 않는다. 모든 접근은 백엔드를 경유한다.
- **범용 테이블명·컬럼명·SQL 문자열을 입력으로 받는 API를 만들지 않는다.**
  "관리자용 쿼리 실행기" 류의 엔드포인트를 두지 않는다.
- 요청과 응답에 DTO를 사용하고, 변경 가능한 필드는 **allowlist**로 명시한다.
  모델 객체를 그대로 노출하거나 임의 필드를 일괄 반영하지 않는다.
- 권한은 **default-deny**와 **least privilege**를 적용한다. 명시적으로 허용된
  주체·행위·대상 조합만 통과한다.
- 새 테이블이 생겼다고 해서 관리자 UI에 자동으로 노출하지 않는다. 노출은
  명시적 등록 행위여야 한다.

## 6. 단일 작성자 원칙

같은 상태에 두 개의 작성자가 존재하면 정합성이 깨진다. 다음을 불변식으로 둔다.

- **Web은 관리 의도를 제출한다.** 실행 상태의 최종 작성자는 `lifelaw-monitor`다.
- Web은 `TN_CRAWL_TARGET`과 `TN_BATCH_RUN`의 실행 상태를 직접 수정하지 않는다.
- 정책 변경은 D-28 `POLICY_BARRIER` 계약을 준수하는 **승인된 명령**만 사용한다.
  정책 원본 테이블과 상속된 하위 값을 별도 트랜잭션으로 따로 갱신하지 않는다.
- 재처리, 취소, 재동기화는 상태를 직접 뒤집는 대신 **관리 명령 계약**을 통해
  요청한다. 수집기가 그 요청을 읽어 실제 상태 전이를 수행한다.
- 낙관적 잠금이 필요한 경로에서는 기대 버전을 함께 제출하고, 불일치 시 실패로
  처리한다. 조용히 덮어쓰지 않는다.

## 7. 보안과 감사

- 인증과 인가를 **모든 요청에서** 검증한다. 프론트엔드의 메뉴 숨김은 인가가
  아니다.
- 관리자 변경에는 **변경 사유**를 기록한다.
- 고위험 작업은 **재인증**과 **승인(approval)** 을 요구한다.
- 감사 기록은 **변경 전후 값**을 함께 남긴다. 누가, 언제, 무엇을, 왜.
- 감사 로그의 수정과 삭제를 금지한다. 애플리케이션 경로에 UPDATE/DELETE를
  두지 않고, DB 권한도 그에 맞춰 최소화한다.
- 비밀정보와 자격증명을 Git에 저장하지 않는다. 배포 환경 secret으로 주입한다.
  설정 파일에는 비밀이 아닌 endpoint만 둔다.
- **DB 소유자 계정을 애플리케이션에서 재사용하지 않는다.** 런타임 계정은
  필요한 최소 권한만 갖는다.
- 외부에서 들어온 값(URL, host, 파일 경로, 식별자)은 신뢰하지 않는다.
  정규화와 검증을 거친 뒤에만 사용한다.
- 오류 메시지에 내부 경로, DSN, 스택트레이스, 자격증명을 노출하지 않는다.

## 8. Git과 worktree

- 기존 작업을 보존한다. 추적·미추적·staged·unstaged 사용자 변경을 임의로
  정리하거나 덮어쓰지 않는다.
- **사용자 승인 없이** commit, push, PR 생성, merge, 원격 저장소 생성, 브랜치
  삭제, 배포를 수행하지 않는다.
- 기본 통합 브랜치는 **`feature`** 다. 일반 작업 브랜치는 `feature`에서 분기한다.
- 장기 브랜치 `feature`가 존재하므로 Git ref 구조상 `feature/<name>`을 만들 수
  없다. 작업 브랜치는 다음 namespace를 사용한다.

  ```text
  task/<slug>    일반 작업
  fix/<slug>     결함 수정
  urgent/<slug>  운영 긴급 수정
  ```

- 승격 흐름: `task/* → feature → develop → release → main`
- 긴급 수정 흐름: `main → urgent/* → hotfix → main`, 이후 `release → develop →
  feature`로 역반영
- **merge commit을 사용한다. squash와 rebase로 프로젝트 이력을 재작성하지
  않는다.**
- 병렬 쓰기 작업은 별도 브랜치와 worktree에서 수행한다. 같은 파일 또는 같은
  의미 경계(DB 계약, 상태 전이, RBAC, 공통 문서)는 **직렬화**한다.
- worktree가 필요하지 않으면 만들지 않고 그 이유를 보고한다.
- 최종 식별자와 문서 버전을 미리 선점하지 않는다.

## 9. 검증과 완료 보고

- 구현 전에 권위 문서와 데이터 계약을 확인한다.
- 변경 후에는 diff, 테스트, 보안, 권한, 계약 드리프트를 검증한다.
- 가장 작고 결정적인 검사부터 수행하고, 경계를 넘는 변경에서만 범위를 넓힌다.
- **명령 출력이나 재현 가능한 증거 없이 `PASS`, 완료, 준수를 보고하지 않는다.**
- 실행하지 않은 검증은 항목과 이유를 함께 보고한다.
- 완료 보고에는 다음을 포함한다.
  - 결과와 변경된 파일
  - 근거가 된 권위 문서와 결정
  - 실행한 정확한 명령과 결과
  - 실행하지 않은 검증과 이유
  - 가정, 이탈, 미해결 질문, 잔여 위험
  - 사용자에게 남은 승인 사항

## 10. 중단 조건

다음 상황에서는 임의로 결정하지 말고 중단하고 보고한다.

- 권위 문서 간 충돌을 발견했고 추론 없이 해소할 수 없을 때
- CUBRID 쓰기 또는 R 계약 변경이 필요해질 때
- 공통 코드나 상태 코드를 새로 만들어야 할 때
- 이력·감사 데이터의 수정·삭제가 필요해질 때
- 승인된 범위를 넘는 구현이 필요할 때
- 병렬 작업의 파일 또는 의미 경계가 겹칠 때
- 되돌리기 어렵거나 파괴적인 동작이 필요할 때
- 원격 저장소 정보나 배포 대상 정보가 필요할 때
